parameters:
  - in: path
    name: companyId
    required: true
    schema:
      type: integer
    description: 회사 id

  - name: Authorization
    in: header
    required: true
    description: Bearer 액세스 토큰
    schema:
      type: string

patch:
  $ref: ./patch.yaml

delete:
  $ref: ./delete.yaml