parameters:
  - in: path
    name: customerId
    schema:
      type: integer
    description: 고객의 id

get:
  security:
    - bearerAuth: []
  $ref: ./get.yaml

patch:
  security:
    - bearerAuth: []
  $ref: ./patch.yaml

delete:
  security:
    - bearerAuth: []
  $ref: ./delete.yaml
