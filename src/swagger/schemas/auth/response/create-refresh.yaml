type: object
properties:
  refreshToken:
    $ref: ../auth.yaml#/properties/refreshToken

  accessToken:
    $ref: ../auth.yaml#/properties/accessToken

required:
  - refreshToken
  - accessToken

additionalProperties: false
