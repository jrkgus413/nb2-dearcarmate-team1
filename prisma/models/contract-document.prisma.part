model ContractDocument {
  id          BigInt   @id @default(autoincrement())
  contractId  BigInt   
  fileId      BigInt
  contract    Contract @relation(fields: [contractId], references: [id], onDelete: Cascade)
  file        Contract @relation(fields: [fileId], references: [id], onDelete: Cascade)

  @@index([contractId])
}
