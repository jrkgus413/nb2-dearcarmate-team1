model Company {
  id BigInt @id @default(autoincrement())
  company_code String @db.VarChar(50) @unique
  name String @db.VarChar(50)
  created_at   DateTime @default(now()) @db.Timestamp()
  updated_at    DateTime @updatedAt @db.Timestamp()

  Customers Customers[]
}
